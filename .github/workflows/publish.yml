name: publish npm package

on:
  push:
    branches: ["main"]

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: login
        run: npm login --registry=https://npm.pkg.github.com --scope=@phanatic
      - name: push
        run: npm publish
